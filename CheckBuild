pipeline {
	agent any
	def device='iPhone 7'
	stages {
		stage('Running Test'){
			agent {
				label "localku"
			}
			steps {
				echo 'Running important automation ...'
      			sh './gradlew clean test -Ddevice='+device+' -DplatformVersion=10.2'
			}
		}
	}
}
